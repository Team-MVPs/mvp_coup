import React from 'react';
import {Card} from 'react-bootstrap';

function Duke(props) {

    return (
        <Card br='primary' style={{ width: '18rem' }}>
            Duke...
        </Card>
    );
}

export default Duke;
